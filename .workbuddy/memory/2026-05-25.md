# 2026-05-25

## 多平台投递按钮功能
- 修改了 `index.html` 中 `showJobsModal` 函数
- 移除整张岗位卡片的 `onclick` 跳转（原只跳 Boss 直聘）
- 新增 `buildJobLinks(title, city)` 函数，集中生成三个平台的搜索链接
- 每张岗位卡片底部新增"去投递"按钮行：BOSS直聘、前程无忧、智联招聘
- 元信息行文案从"点击搜岗位 →"改为"选择平台去投递 ↓"
- 新增约 40 行 CSS 样式：`.job-apply-row` / `.job-apply-label` / `.job-apply-btn`
- 三个平台 URL 模式：
  - BOSS直聘: `https://www.zhipin.com/job_detail/?query=${kw}&city=${city}`
  - 前程无忧: `https://we.51job.com/pc/search?keyword=${kw}`
  - 智联招聘: `https://www.zhaopin.com/sou/?kw=${kw}`
- 未改动：后端、API、config.js、上传简历、AI 分析、聊天、投递追踪逻辑

## 语音面试 MVP 可行性分析 → 已撤回，改为精简方案
- 撤回：index.html 中 VoiceInterview 模块和 .vi-* CSS 已删除
- 撤回：showInterviewModal() 恢复为原始占位弹窗（跳转到 index-original-backup.html）
- 精简实现改为在 index-original-backup.html 的面试 textarea 旁加麦克风按钮

## 语音输入功能（精简方案）
- 只修改了 `index-original-backup.html`，共 3 处：
  - CSS：新增 .agent-mic-btn / .agent-mic-btn.recording / .agent-mic-unsupported（约 20 行）
  - HTML：在 #agentInput textarea 旁边新增麦克风按钮和降级提示
  - JS：新增 SpeechInput IIFE 工具函数（约 60 行），纯语音转文字填入输入框
- 不完全保留原来的面试流程：textarea → sendInterviewChat() → /chat/stream
- 不支持语音 → 按钮隐藏，显示提示
- 未改动：sendInterviewChat、interviewState、SSE 流、岗位选择、JD 粘贴、后端、API、config.js

## 语音输入优化
- 修复 SpeechInput 模块：
  - `recognition.continuous = false` 显式设置
  - 录音中按钮 title 显示"正在听，请开始说话..."
  - no-speech 等错误通过 #micUnsupported span 显示用户可见提示
  - 识别成功/开始录音时自动清除错误提示
  - 每次启动录音前清除上一轮错误
